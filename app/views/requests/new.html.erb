<div class="new_request_page">

  <h1> New Request </h1>

    <div class="new_request_form">

      <%= form_for @request, url: {action: "create"} do |form| %>

        <% if @request.errors.any? %>
          <div id="error_explanation">
            <h4><%= pluralize(@request.errors.count, "error") %> prohibited this request from being saved:</h4>

            <ul>
              <% @request.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>

          </div>
        <% end %>

        <% if @user.errors.any? %>
          <div id="error_explanation">
            <h4><%= pluralize(@user.errors.count, "error") %> prohibited this request from being saved:</h4>

            <ul>
              <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>

          </div>
        <% end %>

        <div class="form_field">
          <%= form.label :Category %>
          <%= form.collection_select :category_id, @categories, :id, :name, include_blank: true %>
        </div>

          <br>

        <div class="form_field">
          <%= form.label :description %>
          <%= form.text_area :description %>
        </div>

          <br>

        <div class="form_field">
          <%= form.label :expire %>
          <%= form.date_field :expire %>
        </div>

          <br>

        <div class="form_field">
          <%= form.label :points %>
          <%= form.number_field :points %>
        </div>

          <br>

        <div class="submit">
          <%= form.submit %>
        </div>

      <% end %>

    </div>
  </div>
