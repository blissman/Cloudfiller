
          <div class="active_request">
            <p>
              <span>Category: &emsp; </span> <%= @request.category.name %>
                <br>
              <span>Description: &emsp; </span> <%= @request.description %>
                <br>
              <span>Points: &emsp; </span> <%= @request.points %>
                <br>
              <span>Expiry: &emsp; </span> <%= @request.expire %>
            </p>
          </div>

          <h2>Responses</h2>

            <% @responders.each do |responder| %>
              <div class="responder_name_rating">

                <p><%= responder.name %> <%= responder.lastname %>
                    <% if responder.ratings.average(:rating) == nil %>
                      (Rating Pending)
                    <% else %>
                      <br> Rating: <%= '%.2f' % responder.ratings.average(:rating) %>/5
                </p>
                    <div class="star-ratings-css">
                      <div class="star-ratings-css-top" style="width: <%= responder.ratings.average(:rating)/5.0*100.0 %>%">
                        <span>★</span><span>★</span><span>★</span><span>★</span><span>★</span>
                      </div>
                      <div class="star-ratings-css-bottom">
                        <span>★</span><span>★</span><span>★</span><span>★</span><span>★</span>
                      </div>
                    </div>
                  <% end %>
                    <br>
                  <!-- Link to Bio Button -->
                  <div id="responder_bio_form_btn">
                      <%= button_to 'Bio', requests_path, remote: true %>
                  </div>

              </div>
            <% end %>

            <!-- <button id="done_request_btn"> <%#= link_to "Done", request_path(@active_request), remote: true %> </button> -->
