
    <strong>Responders:</strong> <br>
    <% request.responses.each do |response| %>
      Name: <%= response.user.name %> <%= response.user.lastname %>
        <br>
      Rating:
      <% if response.user.ratings.average(:rating) == nil %>
        (Rating Pending)
      <% else %>
        <%= response.user.ratings.average(:ratings) %>
      <% end %>
        <br>
    <% end %>
  <br>
