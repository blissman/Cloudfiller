<% if activity.owner == current_user %>

  messaged <%= activity.recipient.name %> about:

    <% if activity.trackable %>
      <%#= link_to activity.trackable.request, activity.trackable %>
      <%= Request.find(activity.trackable.request_id).description %>
    <% else %>
      which has since been removed
    <% end %>

<% else %>

wants to start a conversation with you about:

  <% if activity.trackable %>
    <%#= link_to activity.trackable.request, activity.trackable %>
    <%= Request.find(activity.trackable.request_id).description %>
  <% else %>
    which has since been removed
  <% end %>

<% end %>
