<% if activity.owner == nil && activity.trackable %>

  The request <%= activity.trackable.description %> has expired.

<% else %>

  completed a request:

  <% if activity.trackable %>
    <%= activity.trackable.description %>
  <% else %>
    which has since been removed
  <% end %>

<% end %>
